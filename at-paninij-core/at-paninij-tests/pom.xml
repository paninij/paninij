<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.paninij</groupId>
    <artifactId>at-paninij-core</artifactId>
    <version>0.0.1-SNAPSHOT</version>
  </parent>

  <artifactId>at-paninij-tests</artifactId>
  <packaging>jar</packaging>

  <name>@PaniniJ Tests</name>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <generated.sources.directory>
      ${project.build.directory}/generated-sources
    </generated.sources.directory>
    <generated.test.sources.directory>
      ${project.build.directory}/generated-test-sources
    </generated.test.sources.directory>
    <project.logsDirectory>
      ${project.basedir}/logs
    </project.logsDirectory>
    <project.soterArgsFile>
      ${project.build.directory}/soter_args_file.txt
    </project.soterArgsFile>
  </properties>


  <build>

    <plugins>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>1.4.0</version>
        <executions>
          <execution>
            <id>runtime-ownership-checks</id>
            <phase>process-classes</phase>
            <goals>
              <goal>java</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <mainClass>org.paninij.soter.Main</mainClass>
          <arguments>
            <argument>-analysisReports</argument>
            <argument>${project.logsDirectory}/soter/analysis-reports</argument>
            <argument>-classOutput</argument>
            <argument>${project.build.outputDirectory}</argument>
            <!--
            <argument>-callGraphPDFs</argument>
            <argument>${project.logsDirectory}/soter/call-graphs</argument>
            -->
            <!--
            <argument>-heapGraphPDFs</argument>
            <argument>${project.logsDirectory}/soter/heap-graphs</argument>
            -->
            <argument>@${project.soterArgsFile}</argument>
          </arguments>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <generatedSourcesDirectory>${generated.sources.directory}</generatedSourcesDirectory>
          <generatedTestSourcesDirectory>${generated.test.sources.directory}</generatedTestSourcesDirectory>
          <fork>true</fork>
          <compilerArgs>
            <arg>-Apanini.soterArgsFile=${project.soterArgsFile}</arg>
          </compilerArgs>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <dependencies>
    <dependency>
      <groupId>org.paninij</groupId>
      <artifactId>at-paninij-code-gen</artifactId>
    </dependency>
    <dependency>
      <groupId>org.paninij</groupId>
      <artifactId>at-paninij-runtime</artifactId>
    </dependency>
    <dependency>
      <groupId>org.paninij</groupId>
      <artifactId>at-paninij-soter</artifactId>
    </dependency>
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
