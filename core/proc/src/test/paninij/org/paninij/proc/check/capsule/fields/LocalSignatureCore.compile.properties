# `StreamCore` needs to be explicitly added as a source. Otherwise, the
# `Stream` interface referred to in `LocalSignatureCore` will not be found.
src = org.paninij.proc.check.capsule.fields.LocalSignatureCore
src = org.paninij.ex.helloworld.StreamCore
errors = yes
