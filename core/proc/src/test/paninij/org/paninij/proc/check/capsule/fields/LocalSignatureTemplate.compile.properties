# `StreamTemplate` needs to be explicitly added as a source. Otherwise, the
# `Stream` interface referred to in `LocalSignatureTemplate` will not be found.
src = org.paninij.proc.check.capsule.fields.LocalSignatureTemplate
src = org.paninij.ex.helloworld.StreamTemplate
errors = yes
